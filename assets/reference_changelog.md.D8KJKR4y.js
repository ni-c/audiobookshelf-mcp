import{_t as e,it as t,n,rt as r}from"./chunks/framework.D9LADW4G.js";var i=JSON.parse(`{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"reference/changelog.md","filePath":"reference/changelog.md","lastUpdated":1787052895000}`),a={name:`reference/changelog.md`};function o(n,i,a,o,s,c){return e(),r(`div`,null,[...i[0]||=[t(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to “Changelog”">​</a></h1><h2 id="_0-4-0-2026-09-07" tabindex="-1">[0.4.0] - 2026-09-07 <a class="header-anchor" href="#_0-4-0-2026-09-07" aria-label="Permalink to “[0.4.0] - 2026-09-07”">​</a></h2><h3 id="security" tabindex="-1">Security <a class="header-anchor" href="#security" aria-label="Permalink to “Security”">​</a></h3><ul><li><strong>Credential fields are removed from every answer.</strong> <code>GET /api/me</code> answers with <code>User.toOldJSONForBrowser()</code>, and Audiobookshelf calls it without <code>hideRootToken</code> — so the document carries <code>token</code>, the account&#39;s old non-expiring access token, for a root account included. The compact projection never named the field, but <code>detail: &quot;full&quot;</code> handed the record on whole, which put a credential outliving this process into a model&#39;s context and into whatever that model&#39;s operator logs. Any field whose normalised name ends in <code>token</code>, <code>password</code>, <code>secret</code>, <code>apikey</code>, <code>passphrase</code>, <code>pash</code> or <code>privatekey</code> is now replaced at any depth, in <code>get_me</code> and in <code>list_bookmarks</code> which reads the same endpoint, and the result names what it removed rather than dropping it silently.</li><li><strong>A confirmation binds every field the dialog names.</strong> <code>update_collection</code> and <code>update_playlist</code> open their dialog on the reorder, because that is the part with no way back — but the same call may carry a name and a description, and those rode along unnamed and unbound. A token issued for &quot;reorder these books&quot; executed a second call that reordered the same books and renamed the collection to something the person never saw. The sentence now names every field the call will write, the caller&#39;s new values are shown on their own labelled lines, and the resource key covers them.</li><li><strong>Emptying a playlist is deleting it, and the allowlist now knows.</strong> Audiobookshelf deletes a playlist outright once its last entry is removed, so <code>AUDIOBOOKSHELF_DENY_TOOLS=delete_playlist</code> did not remove the capability. <code>remove_items_from_playlist</code> reads the playlist before it asks, says in the dialog when the removal takes the last entry, and refuses outright where <code>delete_playlist</code> is not registered.</li><li><strong>The API key cannot leave through an error message.</strong> There was no shape check on <code>AUDIOBOOKSHELF_API_KEY</code> and no check before the request, so a key pasted across two lines reached undici — whose refusal is <code>Headers.append: &quot;&lt;value&gt;&quot; is an invalid header value.</code>, the whole value, which <code>run</code> then answered with. The key is trimmed and checked for shape at startup (printable ASCII, 8 to 4096 characters, the message naming the variable and the length and never the value) and every header is checked again before the request, because a <code>Config</code> can be built without <code>loadConfig</code>.</li><li><strong>Two supply-chain gaps in jobs holding an OIDC token.</strong> The npm publish job ran <code>npm ci</code> without <code>--ignore-scripts</code>, so every dependency&#39;s install hook would have run while the Trusted Publishing token was available; nothing in the tree declares one, which is what makes the flag free. And <code>mcp-publisher</code> was fetched from <code>releases/latest/download</code> in two jobs with <code>id-token: write</code> — it is now pinned to a version and verified against the release&#39;s published checksum. <code>gh release create</code> gained <code>--verify-tag</code>, and pull requests are checked by <code>actions/dependency-review-action</code>.</li><li>Podcast feed URLs are redacted. A private feed is published as <code>https://user:token@host/feed.rss</code> and Audiobookshelf stores it as given, so <code>get_library_item</code> handed the credential back in both channels.</li><li><code>get_library_stats</code> and <code>get_me</code> answer as untrusted content. The first carries the titles and authors of a library&#39;s longest and largest items, the second an account&#39;s bookmark titles and selected tags — all written by somebody other than the operator, and all previously framed as this server&#39;s own words.</li><li><strong>mcp-approval 0.8.2.</strong> A sealed dialog answer is single-use since 0.8.1: the same <code>requestState</code> presented again within its lifetime used to be accepted again, and with a resource key that is the same every time — a whole stream, a fixed set of targets — every replay landed. npm users on <code>^0.8.0</code> already had the fix; the Docker image is built from the lockfile and carried 0.8.0 until this release.</li><li><strong>Approval keys bound to positions.</strong> <code>update_playlist</code> (items) and <code>update_collection</code> (books) reorder a list, and the order is the whole change, so each entry was prefixed with its index by hand before <code>setResourceKey</code> sorted the list. <code>delete_bookmark</code> keyed the pair (item, seconds) as a plain set, so a token for one pairing also matched the swapped one. All three now build their key with <code>orderedResourceKey</code> from mcp-approval 0.8.2, which binds every part to its position itself; the hand-written prefixes are gone. Keys over sets of ids — the removals, the deletions — stay on <code>setResourceKey</code>, where sorting is the point.</li></ul><h3 id="fixed" tabindex="-1">Fixed <a class="header-anchor" href="#fixed" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><strong>Seventeen answers the instance can give that a tool could not.</strong> A <code>200</code> with no body — which several routes legitimately send — was read as an object by six tools and handed to the result budget by three more, where <code>Buffer.byteLength(undefined)</code> became the tool result. A <code>total</code> of <code>null</code> or <code>1e999</code> failed a whole listing, because <code>z.number()</code> refuses the <code>Infinity</code> that <code>1e999</code> parses to. One <code>null</code> among the entries of a <code>detail: &quot;full&quot;</code> list failed the answer with every good entry in it. An empty readback failed <code>set_media_progress</code>, <code>create_bookmark</code> and <code>update_bookmark</code> <em>after</em> the write had happened. Every response now goes through a boundary that shapes it, and a field the instance did not send is absent rather than fatal.</li><li><strong>The result budget is linear again.</strong> It cut one candidate per round and re-serialised the whole document to measure the result, so the number of rounds was the instance&#39;s to choose: 2 000 text fields took 5.4 seconds, 4 000 took 24, and 8 000 took 105 — on the thread that serves every request. Each round now collects what can be cut, spends the largest first until the estimate covers the overshoot, and measures once. The same 8 000 fields take 42 milliseconds. And the search is recursive, so the bulk of a <code>detail: &quot;full&quot;</code> item — <code>media.audioFiles</code>, one level down — is shortened instead of making the tool refuse the answer.</li><li>The base URL is trimmed in one pass. <code>replace(/\\/+$/, &#39;&#39;)</code> is tried from every position of a run of slashes and consumes it each time, so an operator URL ending in 80 000 of them cost 1.7 seconds at startup. It is now 0.</li><li>A <code>__proto__</code> key from the instance no longer loses a count. The budget&#39;s record of dropped entries was an object literal written with <code>record[key] =</code>, which on that one name sets the prototype and drops the field — so the entries were dropped and the note said nothing had been.</li><li>Control characters are stripped from every string and every key a result carries, in both channels, and a lone surrogate is repaired. An escape sequence in a book title could repaint the log of whoever read the result, and a lone surrogate — legal JSON — makes a Python client raise <code>UnicodeEncodeError</code>. Bidirectional marks and joiners are kept: they are content in a title.</li><li>Backend text quoted into an error is cut, stripped and labelled as the instance&#39;s words: a media type that is not <code>book</code>, a content type that is not JSON, and every error body. An error body is read under a 64 KiB ceiling of its own rather than the 5 MB one, so a 401 answered with a login page costs what it should.</li><li><code>remove_items_from_playlist</code> validates its ids before it asks about them. The path check ran after the approval, so a person was asked about ids that had not been checked and an approval was spent on a call that could not run.</li><li>Caller arguments are bounded: ids at 128 characters, a search query at 500, a sort key at 100, a filter value at 1 000, <code>page</code> at a million and a playback position at 10^9. Each of them reaches a URL path, a confirmation sentence or an error message.</li><li><code>-0</code> no longer splits the two channels of an answer: <code>JSON.stringify</code> writes it as <code>0</code> while <code>structuredContent</code> keeps it.</li></ul><h3 id="added" tabindex="-1">Added <a class="header-anchor" href="#added" aria-label="Permalink to “Added”">​</a></h3><ul><li>The server introduces itself in full. <code>title</code>, <code>description</code>, <code>websiteUrl</code> and <code>icons</code> now travel with <code>name</code> and <code>version</code>, so a client that shows a server to a person has something to show. All four were already in <code>server.json</code> for the registry and reached no client at all; a test compares the two so they cannot drift.</li><li>Server <code>instructions</code>. Results carry an <code>untrusted</code> marker, but that is read after the fact — this is the channel a model sees before it calls anything.</li><li>An OpenSSF Scorecard run, weekly and on every push to <code>main</code>, reporting into the Security tab next to CodeQL and Trivy. The badge is the second in the row.</li></ul><h3 id="changed" tabindex="-1">Changed <a class="header-anchor" href="#changed" aria-label="Permalink to “Changed”">​</a></h3><ul><li><code>docs/reference/tools.md</code> is written by hand again. It used to be generated from the registered tools, which kept it in step with the code at the price of a page nobody could edit: <code>--check</code> compared it byte for byte, so every line had to be derivable and a paragraph about how an endpoint really behaves had nowhere to go. A test now asserts what the generator guaranteed — the page documents exactly the tools that exist, marks exactly the <code>essential</code> preset, and marks exactly the tools that ask a person first — and leaves the prose to a person.</li><li><code>homepage</code> in <code>package.json</code> points at the documentation site rather than at the README anchor on GitHub. It is what npm shows next to the package, and every one of these servers has had a documentation site for weeks.</li><li>Source maps are no longer published in the npm tarball. Node reads them only under <code>--enable-source-maps</code>, which nothing here sets, and the maps pointed at a <code>src/</code> this package does not ship — so a stack trace under that flag named a file nobody could open. <code>dist/**/*.js</code> is unchanged; the package is about a fifth smaller.</li><li>yarn, corepack and the lockfile are gone from the runtime image, for the same reason npm already was: nothing reaches them from <code>node dist/index.js</code>, and each carries a vendored dependency tree for a scanner to find.</li></ul><h2 id="_0-3-0-2026-09-03" tabindex="-1">[0.3.0] - 2026-09-03 <a class="header-anchor" href="#_0-3-0-2026-09-03" aria-label="Permalink to “[0.3.0] - 2026-09-03”">​</a></h2><h3 id="added-1" tabindex="-1">Added <a class="header-anchor" href="#added-1" aria-label="Permalink to “Added”">​</a></h3><ul><li><p>Every tool declares an <code>outputSchema</code> and answers with <code>structuredContent</code> beside the text block. A client no longer has to parse prose to use a result — which seven of them made unavoidable, since they answered with a sentence.</p><p>The tools that report library metadata carry <code>untrusted: true</code> and <code>source: &quot;audiobookshelf&quot;</code> as fields, not only as a preamble in the text. Book descriptions pulled from metadata providers, podcast feed summaries and episode titles are written by someone else, so a client that reads the structured half must not get them unframed.</p><p>The documents are described as open objects with the top-level keys this server builds. <code>detail: &quot;full&quot;</code> hands the API record back whole, so a strict shape would turn that mode into a failed call.</p></li><li><p>Tools that need a confirmation now <strong>ask the user</strong>, on clients that can show a prompt. The two-call <code>confirm_token</code> remains for clients that cannot, so nothing that works today stops working — but where a person can be asked, one is, instead of a token that only proves the same call was made twice.</p></li><li><p><strong>Three more tools ask before they act</strong>, all of which carried <code>destructiveHint: true</code> and went through unannounced: <code>delete_bookmark</code>, <code>remove_books_from_collection</code> and <code>remove_items_from_playlist</code>.</p><p>They were exempt because they could be undone, and that turned out to be only half true. <code>add_books_to_collection</code> appends at the end rather than restoring an order. <code>create_bookmark</code> makes a new bookmark at a position, with a new title — the one somebody typed is gone, and the position is the only thing <code>delete_bookmark</code> is given, so a wrong <code>time</code> takes out a different bookmark than the one that was meant. And Audiobookshelf deletes a playlist outright once its last entry is removed, which <code>remove_items_from_playlist</code> already warned about <em>after</em> the fact.</p><p><code>delete_bookmark</code>&#39;s description said in so many words &quot;No confirmation token: a bookmark is a position and a title, and create_bookmark restores it.&quot; It does not restore the title.</p></li><li><p><code>ELICITATION</code> switches the dialog off — <code>false</code> sends a client that could have been asked down the two-call-token path instead. For a scheduled job or a test harness, where a dialog is the wrong shape rather than an unwanted one.</p><p>It does <strong>not</strong> remove the guard: there is no setting in which a guarded call goes unannounced. Two deliberate rough edges come with it. The variable is <strong>not prefixed</strong>, so one <code>export ELICITATION=false</code> reaches every MCP server in the environment — which is why a server started with it off prints a line saying so, and why the fallback text names the server instead of blaming a client that was working fine. And a value that is neither <code>true</code> nor <code>false</code><strong>stops the server</strong>: it is the only variable here that defaults to <em>on</em>, so failing open on a typo would leave the dialog running while the operator believed it was off. It is read after the API key is wiped from the environment, so that exit cannot leave the key behind.</p></li><li><p>A <code>docs/guide/approval.md</code> page, and a 👤 marker in the generated tool reference that is read off the registered schema rather than from a list kept beside it.</p></li></ul><h3 id="changed-1" tabindex="-1">Changed <a class="header-anchor" href="#changed-1" aria-label="Permalink to “Changed”">​</a></h3><ul><li><p>The advertised schemas avoid a spelling that is legal JSON Schema and still gets a tool refused, or its constraint silently dropped, by some MCP clients: an open object now writes <code>&quot;additionalProperties&quot;: true</code> rather than the empty schema <code>{}</code> zod emits for it. What the tools accept and return is unchanged; only the way the schema says so is.</p></li><li><p><code>get_personalized_shelves</code> answers <code>{items: [...]}</code> instead of the bare array the API sends. A schema whose root is an array is served to a 2025-era client rewritten as <code>{result: …}</code>, so the tool would otherwise answer in two shapes depending on which revision the client spoke.</p></li><li><p>A result too large to shrink is an error rather than an envelope saying so. The envelope was a different shape from what the tool declares it returns, which the SDK refuses.</p></li><li><p>The two-call <code>confirm_token</code> prompt is an error result. What was asked for did not happen, which is what <code>isError</code> says. The text is unchanged and still carries the token.</p></li><li><p>The integration compose file publishes Audiobookshelf on <code>AUDIOBOOKSHELF_PORT</code> (default 13378) instead of a hardcoded 13378, so a workstation that already runs one there does not need a patched compose file.</p></li><li><p>Runs on <strong>MCP SDK 2.0</strong>. Existing clients see the same protocol revision they always did; the change is the package layout behind it, and it is what lets the dialog above work on both protocol eras from one code path — including behind a stateless gateway, where the older mechanism silently fell back to the weaker token for every client.</p></li><li><p>The linter is <strong>oxlint</strong> instead of eslint plus typescript-eslint, which lifts the TypeScript ceiling: typescript-eslint pins <code>typescript</code> below 6.1, so this repository was held on TypeScript 6 by its linter rather than by its code.</p></li><li><p>The tool filter, the confirmation store, the host classifier and the documentation-asset generator now come from <strong><code>mcp-tool-allowlist</code></strong>, <strong><code>mcp-approval</code></strong>, <strong><code>mcp-internal-hosts</code></strong> and <strong><code>svg-asset-set</code></strong> rather than from copies kept here — 872 fewer lines, and one place to fix each. None of them has a runtime dependency of its own.</p></li><li><p>The shared libraries move to <code>mcp-approval</code> 0.7.1, <code>mcp-tool-allowlist</code> 0.2.1, <code>mcp-internal-hosts</code> 0.2.1, <code>mcp-integration-harness</code> 0.2.0 and <code>svg-asset-set</code> 0.2.0.</p></li><li><p><code>SECURITY.md</code> now says what the confirmation <strong>proves</strong>: binding to one operation with one set of arguments, not freshness. No replay defence is built, because the sealing key is per process, the token is single-use, and <code>requestState</code> only crosses the wire on protocol revision <code>2026-07-28</code>, which this server does not offer — it takes the SDK&#39;s default list, which ends at <code>2025-11-25</code>. The section names what would have to change for that to stop being true.</p></li><li><p>stdio is served through <code>serveStdio</code>, so the connection&#39;s era is negotiated on the opening exchange rather than assumed. A client that pins the <code>2026-07-28</code> era is served it; until now its <code>server/discover</code> probe was answered with &quot;Method not found&quot; and only <code>2025-11-25</code> was on offer. A client that speaks the older era sees no change — it is still pinned to one instance for the life of the connection, exactly as a hand-wired <code>StdioServerTransport</code> served it.</p></li></ul><h3 id="fixed-1" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-1" aria-label="Permalink to “Fixed”">​</a></h3><ul><li><p><strong>A result ceiling of 100 000 bytes</strong>, applied in <code>jsonResult</code> and <code>untrustedJsonResult</code> rather than per tool — so <code>detail: &quot;full&quot;</code>, which switches the compact projections off, is covered by it too. Whole entries are dropped, never characters: a truncated document is not a smaller answer, it is an unparseable one. The result carries a <code>truncated</code> block naming what to call instead, and the budget counts <strong>bytes</strong>, because a library of CJK-titled books is roughly three bytes per counted UTF-16 unit.</p></li><li><p><strong>A compact collection or playlist embeds at most 25 members.</strong><code>compactCollection</code> mapped <code>books</code> unconditionally — not behind <code>detail</code>, not behind a count — and <code>compactPlaylist</code> did the same while embedding a full compact <code>libraryItem</code> <em>and</em> <code>episode</code> per entry. Forty collections of three hundred books was twelve thousand embedded objects in one default-detail read tool. <code>numBooks</code> and <code>numItems</code> still report the real count, and <code>get_collection</code> / <code>get_playlist</code> return the whole membership for one of them.</p></li><li><p><strong><code>update_collection</code> and <code>update_playlist</code> documented an effect they do not have.</strong> Both said their list argument &quot;replaces the order completely, so it has to contain every item that should stay&quot;, which reads as &quot;an item you leave out is removed&quot;. Measured against Audiobookshelf 2.29.0, neither can change membership at all:</p><ul><li><code>PATCH /api/collections/{id}</code> treats <code>books</code> as a <strong>sort key over the rows the collection already has</strong> — the controller loads them and orders them by <code>findIndex</code> in the payload. An id that is not in the collection is ignored, and a book left out gets index <code>-1</code> and moves to the <strong>front</strong> rather than being removed. A payload of ids that do not exist answers <code>200</code> and changes nothing.</li><li><code>PATCH /api/playlists/{id}</code> refuses a list whose length differs from the playlist&#39;s, with <code>400 Invalid playlist items. Length mismatch</code>.</li></ul><p>Both descriptions now say what the arguments do, including the front-of-list behaviour and the 400. The integration suite pins both against a real instance — a stub agrees with any semantics at all, which is how this survived.</p></li><li><p><strong>The README&#39;s &quot;list tools are capped at 100 entries per call&quot; was true for one tool.</strong> Seven of the fourteen listing tools have no <code>limit</code> at all: <code>list_libraries</code>, <code>list_authors</code>, <code>list_tags</code>, <code>list_genres</code>, <code>list_collections</code>, <code>list_playlists</code> and <code>list_bookmarks</code>. The claim now says what holds — a response ceiling, a result ceiling and an embedded-member cap — and names the seven.</p><p>No client-side <code>limit</code>/<code>page</code> was added to them: the Audiobookshelf routes behind those seven take no paging parameters and answer with everything at once, so a <code>page</code> argument would advertise server paging that does not exist and would refetch the whole payload for each page.</p></li><li><p><code>AUDIOBOOKSHELF_READ_ONLY</code> accepts <code>1</code>, <code>true</code> and <code>yes</code>, trimmed and case-insensitively, where it used to require the exact string <code>true</code>. It fails <em>towards</em> the restriction, so <code>AUDIOBOOKSHELF_READ_ONLY=1</code> silently registering the write tools is the one outcome it must not have. <code>AUDIOBOOKSHELF_INSECURE_TLS</code> keeps the exact-match rule, for the same reason read the other way round.</p></li><li><p>Confirmation tokens are compared with a <strong>constant-time</strong> comparison. The copy in this repository used <code>!==</code>, which leaks through timing how much of a guess was right. Reaching a token still requires having received it in a previous tool result, so this closes a margin rather than a hole.</p></li><li><p>An entry in <code>AUDIOBOOKSHELF_ALLOW_TOOLS</code> that is not tool-name-shaped is now <strong>redacted</strong> in the error rather than quoted back. <code>AUDIOBOOKSHELF_API_KEY</code> and <code>AUDIOBOOKSHELF_ALLOW_TOOLS</code> are adjacent lines in every compose file, and a paste into the wrong one used to print the credential into the client&#39;s log.</p></li></ul><h3 id="security-1" tabindex="-1">Security <a class="header-anchor" href="#security-1" aria-label="Permalink to “Security”">​</a></h3><ul><li><p><strong><code>update_collection</code> and <code>update_playlist</code> ask before they reorder.</strong><code>remove_books_from_collection</code> sits behind a dialog plus a token, and the consequence it names is not the membership — it is that &quot;the curated order of the collection cannot be reconstructed from here&quot;. Reordering does exactly that and nothing else, and it went through with no question at all. The gate ran between <strong>verbs</strong> where the risk runs between <strong>effects</strong>.</p><p>The gate is conditional on <code>library_item_ids</code> / <code>items</code> being present. Renaming and re-describing still ask nothing: those are recoverable by typing the old text back, and a dialog in front of every rename is how people learn to tick without reading.</p><p>The confirmation key carries each target&#39;s <strong>position</strong>, not just the set: <code>setResourceKey</code> sorts before fingerprinting, so a bare list of ids would have given <code>[A, B]</code> and <code>[B, A]</code> the same key — and the order is the whole change.</p><p>Chosen over removing the argument and adding guarded <code>reorder_*</code> tools: that is the same guard, two more tools, and a breaking change to a documented schema.</p></li><li><p><strong>Five routes are exempted from that check explicitly, not by weakening it.</strong><code>DELETE /api/collections/{id}</code>, <code>DELETE /api/playlists/{id}</code>, <code>PATCH /api/me/progress/{id}</code>, <code>DELETE /api/me/progress/{id}</code> and <code>DELETE /api/me/item/{id}/bookmark/{time}</code> answer <code>200 text/plain &quot;OK&quot;</code> on 2.29.0. All five are mutations whose caller ignores the value, so each says so at the call site. Which five could only be established against a real instance.</p></li><li><p><strong>A 200 that is not JSON is now an error instead of an empty list.</strong> The body used to be returned as a string, and a string finds neither an array nor an envelope in <code>listFrom</code> — so an SSO portal, a captive proxy or a misconfigured reverse proxy in front of the instance made <code>list_libraries</code> answer &quot;you have no libraries&quot;. A swallowed error replaced by a plausible wrong answer is worse than an error.</p><p>Second fuse for the same failure: the base URL is built from the parsed URL rather than from the raw string. <code>AUDIOBOOKSHELF_URL=https://abs.example.com/#dev</code> passed validation, lost everything from the <code>#</code> onwards in <code>fetch</code>, and sent every request — bearer token attached — to <code>/</code>, where the web UI answers 200 with HTML. A query string went the same way one character earlier.</p></li><li><p><strong>Responses are read under a 5 MB ceiling.</strong> <code>content-length</code> is checked before a byte is read, and a chunked body — which declares no length — is counted while reading and cut off. <code>/api/collections</code> takes no paging parameters and embeds every book of every collection, so a shared server with forty collections of three hundred books answered in double-digit megabytes, which <code>response.text()</code> and then <code>JSON.parse</code> held about three copies of. An error body is still read (truncated), because the status code is the diagnostic and a size complaint would replace it.</p></li></ul><h2 id="_0-2-0-2026-08-27" tabindex="-1">[0.2.0] - 2026-08-27 <a class="header-anchor" href="#_0-2-0-2026-08-27" aria-label="Permalink to “[0.2.0] - 2026-08-27”">​</a></h2><h3 id="added-2" tabindex="-1">Added <a class="header-anchor" href="#added-2" aria-label="Permalink to “Added”">​</a></h3><ul><li><p><code>AUDIOBOOKSHELF_ALLOW_TOOLS</code> and <code>AUDIOBOOKSHELF_DENY_TOOLS</code> choose which of the 44 tools are registered. Both take comma-separated tool names or a prefix with a trailing <code>*</code>, the allow list decides what is in and the deny list is subtracted from it, and <code>AUDIOBOOKSHELF_ALLOW_TOOLS=essential</code> selects a curated eight — <code>list_libraries</code>, <code>search_library</code>, <code>list_library_items</code>, <code>get_library_item</code>, <code>get_item_chapters</code>, <code>list_items_in_progress</code>, <code>get_media_progress</code>, <code>set_media_progress</code>. A model picks the right tool far more reliably from eight than from forty-four, and every visible tool costs context on every request. Nothing changes for an installation that sets neither.</p><p>A filtered tool is not registered at all, so it is absent from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot; — the same cut <code>AUDIOBOOKSHELF_READ_ONLY</code> already makes, not a second, weaker one.</p><p>An entry that matches no tool <strong>stops the server at startup</strong>, naming the entry and listing the real names, rather than being ignored: an ignored typo leaves a tool missing from <code>tools/list</code> with nothing pointing at the cause.</p></li></ul><h3 id="changed-2" tabindex="-1">Changed <a class="header-anchor" href="#changed-2" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The README now carries the same eight badges, in the same order, as every other MCP server in this family, all of them reading from npm rather than hard-coded; the opening follows one shape; and the standalone &quot;Full documentation&quot; line is gone, because the docs badge three lines above it points at the same page.</li></ul><h3 id="fixed-2" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-2" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The container image no longer ships OpenSSL 3.5.7-r0, which carries <strong>CVE-2026-14456</strong> (denial of service via unbounded memory growth). The pinned <code>node:24-alpine</code> digest is already the newest one; Alpine&#39;s fixed 3.5.8-r0 has simply not been rebuilt into it yet, so the runtime stage now upgrades <code>libcrypto3</code> and <code>libssl3</code> by name. Upgrading those two rather than running a blanket <code>apk upgrade</code> keeps the rest of the image exactly as the digest pins it. The step can go once the base image ships the fix.</li></ul><h2 id="_0-1-4-2026-08-26" tabindex="-1">[0.1.4] - 2026-08-26 <a class="header-anchor" href="#_0-1-4-2026-08-26" aria-label="Permalink to “[0.1.4] - 2026-08-26”">​</a></h2><h3 id="changed-3" tabindex="-1">Changed <a class="header-anchor" href="#changed-3" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The check that decides whether <code>AUDIOBOOKSHELF_URL</code> points somewhere local — and therefore whether sending a credential over plain <code>http</code> is worth warning about — now uses the same host classifier as the other MCP servers in this family, in <code>src/hosts.ts</code>. The string comparison it replaces missed several spellings of the same address: <code>http://[::ffff:127.0.0.1]</code>, which <code>URL</code> canonicalises to <code>[::ffff:7f00:1]</code> before any check sees it, and <code>localhost.</code> with its root label. It also treated <code>127.example.com</code> as loopback, because it matched on the <code>127.</code> prefix, and so stayed quiet about a plain-http URL to a public host.</li></ul><p>Nothing else changes: this server has no tool that takes a URL, so there is no request whose target a caller can choose.</p><h2 id="_0-1-3-2026-08-18" tabindex="-1">[0.1.3] - 2026-08-18 <a class="header-anchor" href="#_0-1-3-2026-08-18" aria-label="Permalink to “[0.1.3] - 2026-08-18”">​</a></h2><h3 id="fixed-3" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-3" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The API key is no longer left in the environment when <code>AUDIOBOOKSHELF_URL</code> is unset. <code>loadConfig</code> deleted it only at the very end, behind the early return for a missing URL, so in that state the key stayed in <code>process.env</code> for the whole process lifetime — readable in <code>/proc/&lt;pid&gt;/environ</code> and inherited by every child process. The deletion now happens before any branch.</li><li>A malformed <code>AUDIOBOOKSHELF_URL</code> is no longer echoed into the log. That branch fires precisely when the variable does not hold a URL, which most often means the API key was pasted into the wrong variable.</li><li><code>http://[::1]:…</code> no longer produces the &quot;plain http to a non-local host&quot; warning. <code>URL.hostname</code> keeps the brackets around an IPv6 literal, so the loopback check never matched that notation.</li></ul><h2 id="_0-1-2-2026-08-18" tabindex="-1">[0.1.2] - 2026-08-18 <a class="header-anchor" href="#_0-1-2-2026-08-18" aria-label="Permalink to “[0.1.2] - 2026-08-18”">​</a></h2><h3 id="fixed-4" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-4" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>The architecture diagram no longer depends on the reader&#39;s operating system. It carried a <code>prefers-color-scheme</code> block, which resolves against the OS rather than the theme toggle of GitHub or npm — so dark-mode readers on a light OS got the light artwork on a dark page. The README now uses <code>&lt;picture&gt;</code>, which is resolved against the page, and the <code>&lt;img&gt;</code> that npm falls back to brings its own card instead of a media query.</li></ul><h3 id="changed-4" tabindex="-1">Changed <a class="header-anchor" href="#changed-4" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The diagram is generated from a single source, <code>docs/assets/architecture.source.svg</code>, by <code>npm run assets</code>. The four rendered copies had already drifted apart; CI now fails if one of them is edited by hand.</li><li><code>docs/public/og.png</code> is generated at exactly 1280x640, GitHub&#39;s recommended size for a social preview, instead of being drawn by hand.</li></ul><h2 id="_0-1-1-2026-08-17" tabindex="-1">[0.1.1] - 2026-08-17 <a class="header-anchor" href="#_0-1-1-2026-08-17" aria-label="Permalink to “[0.1.1] - 2026-08-17”">​</a></h2><p>First release published by the automated pipeline, with npm provenance.</p><h3 id="added-3" tabindex="-1">Added <a class="header-anchor" href="#added-3" aria-label="Permalink to “Added”">​</a></h3><ul><li>Multi-arch container image on GHCR (<code>ghcr.io/ni-c/audiobookshelf-mcp</code>) for linux/amd64 and linux/arm64, built with an SBOM and build provenance.</li><li>Documentation site at <a href="https://audiobookshelf-mcp.ni-c.de" target="_blank" rel="noreferrer">https://audiobookshelf-mcp.ni-c.de</a>, including a complete tool reference generated from the registered tools.</li><li>Listed in the official MCP registry as <code>io.github.ni-c/audiobookshelf-mcp</code>.</li><li><code>SECURITY.md</code> with the trust model, <code>CONTRIBUTING.md</code>, and issue forms.</li></ul><h3 id="changed-5" tabindex="-1">Changed <a class="header-anchor" href="#changed-5" aria-label="Permalink to “Changed”">​</a></h3><ul><li>The runtime image no longer contains npm. It was only ever there because the base image ships it, the entrypoint is plain <code>node</code>, and the dependency tree npm vendors accounted for every HIGH/CRITICAL advisory Trivy reported against the image — none of them in this project&#39;s own dependencies.</li></ul><h3 id="fixed-5" tabindex="-1">Fixed <a class="header-anchor" href="#fixed-5" aria-label="Permalink to “Fixed”">​</a></h3><ul><li>Test coverage raised from 93.9 % to 99.6 % of statements, mostly across the projections that absorb Audiobookshelf&#39;s varying response shapes, and the error paths.</li></ul><h2 id="_0-1-0-2026-08-17" tabindex="-1">[0.1.0] - 2026-08-17 <a class="header-anchor" href="#_0-1-0-2026-08-17" aria-label="Permalink to “[0.1.0] - 2026-08-17”">​</a></h2><h3 id="added-4" tabindex="-1">Added <a class="header-anchor" href="#added-4" aria-label="Permalink to “Added”">​</a></h3><ul><li>Initial release: MCP server for Audiobookshelf.</li><li>29 read tools: libraries, library items with filtering and paging, search, series, authors, personalized shelves, tags, genres, items, chapters, podcast episodes, the current user, listening progress, listening statistics, listening sessions, bookmarks, collections and playlists.</li><li>15 write tools: listening progress, bookmarks, collections and playlists.</li><li><code>AUDIOBOOKSHELF_READ_ONLY=true</code> registers the read tools only.</li><li>Confirmation tokens for the irreversible operations (<code>delete_collection</code>, <code>delete_playlist</code>, <code>delete_media_progress</code>).</li><li>Compact projections for every media response, with <code>detail: &quot;full&quot;</code> for the raw Audiobookshelf object.</li></ul>`,49)]])}var s=n(a,[[`render`,o]]);export{i as __pageData,s as default};