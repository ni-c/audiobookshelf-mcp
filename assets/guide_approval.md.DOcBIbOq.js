import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.qcK96eCy.js";var i=JSON.parse(`{"title":"Asking a person","description":"","frontmatter":{},"headers":[],"relativePath":"guide/approval.md","filePath":"guide/approval.md","lastUpdated":1788348328000}`),a={name:`guide/approval.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="asking-a-person" tabindex="-1">Asking a person <a class="header-anchor" href="#asking-a-person" aria-label="Permalink to “Asking a person”">​</a></h1><p>Eight of the 44 tools can take something out of a list somebody curated, or out of a listening history nobody can reconstruct. All eight <strong>ask a person first</strong>.</p><p>Not a <code>confirm: true</code> argument the model can set. Not a token the model reads out of its own previous result. A dialog, raised through <a href="https://modelcontextprotocol.io/specification/2025-11-25/client/elicitation" target="_blank" rel="noreferrer">MCP elicitation</a>, that goes to the client and is shown to whoever is sitting there.</p><p>The specification says a client <em>should</em> keep a human in the loop:</p><blockquote><p>there <strong>SHOULD</strong> always be a human in the loop with the ability to deny tool invocations</p></blockquote><p>This server does not rely on that. It raises the question itself, and until an answer comes back, nothing happens.</p><h2 id="what-asks" tabindex="-1">What asks <a class="header-anchor" href="#what-asks" aria-label="Permalink to “What asks”">​</a></h2><table tabindex="0"><thead><tr><th>Tool</th><th>What it takes out</th></tr></thead><tbody><tr><td><code>delete_collection</code></td><td>the curated list and its order; the books stay</td></tr><tr><td><code>delete_playlist</code></td><td>the playlist; the media stays</td></tr><tr><td><code>delete_media_progress</code></td><td>position, finished state and dates — the listening history of an item</td></tr><tr><td><code>delete_bookmark</code></td><td>the title typed for a position</td></tr><tr><td><code>remove_books_from_collection</code></td><td>membership, and the order it sat in</td></tr><tr><td><code>remove_items_from_playlist</code></td><td>entries — and the whole playlist, if it was the last one</td></tr><tr><td><code>update_collection</code></td><td>the curated order, when <code>library_item_ids</code> is given — see below</td></tr><tr><td><code>update_playlist</code></td><td>the curated order, when <code>items</code> is given — see below</td></tr></tbody></table><p>The last three were added because &quot;you can just put it back&quot; turned out to be only half true:</p><ul><li><code>add_books_to_collection</code> appends at the end. It does not restore an order.</li><li><code>create_bookmark</code> makes a new bookmark at that position, with a new title. The one somebody typed is gone, and the position is the only thing <code>delete_bookmark</code> is given — a wrong <code>time</code> takes out a different bookmark than the one that was meant.</li><li>Audiobookshelf <strong>deletes a playlist outright</strong> once its last entry is removed.</li></ul><p>The last two are <strong>conditional</strong>, and that is the point of them: the gate hangs off the effect, not off the verb.</p><p><code>update_collection</code> and <code>update_playlist</code> are named as updates, and one of the things they do is take something out — the <strong>order</strong> somebody arranged. That is the very consequence <code>remove_books_from_collection</code> cites for asking (&quot;the curated order of the collection cannot be reconstructed from here&quot;), so an operation whose only effect is that cannot be the cheaper call. Since 0.3.0 they ask whenever <code>library_item_ids</code> / <code>items</code> is present.</p><p>Be clear about what those two arguments do <strong>not</strong> do, because their own descriptions used to imply otherwise. Measured against Audiobookshelf 2.29.0:</p><ul><li><code>PATCH /api/collections/{id}</code> treats <code>books</code> as a <strong>sort key over the membership it already has</strong>. An id that is not in the collection is ignored, and a book left out of the list is not removed — it gets index <code>-1</code> and moves to the <em>front</em>. A list of ids that do not exist at all answers 200 and changes nothing.</li><li><code>PATCH /api/playlists/{id}</code> refuses a list whose length differs from the playlist&#39;s, with <code>400 Invalid playlist items. Length mismatch</code>. It cannot drop an entry even in principle.</li></ul><p>Use <code>remove_books_from_collection</code> and <code>remove_items_from_playlist</code> to change membership. Renaming and re-describing stay free: they are recoverable by typing the old text back, and a dialog in front of every rename is how people learn to tick without reading. Everything else — creating, adding, setting progress — asks nothing: it adds or sets without taking anything away.</p><h2 id="what-the-dialog-contains" tabindex="-1">What the dialog contains <a class="header-anchor" href="#what-the-dialog-contains" aria-label="Permalink to “What the dialog contains”">​</a></h2><p>Ids and counts. Never a title, a name or a description.</p><p>That is not a formatting preference. Book descriptions come from metadata providers, podcast titles from RSS feeds, collection names from whoever uses the server. None of it is written by you, and the prompt is read by a model at the exact moment it is deciding whether to delete something.</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>This will remove 2 book(s) from collection col_abc123.</span></span>
<span class="line"><span></span></span>
<span class="line"><span>The curated order of the collection cannot be reconstructed from here — putting</span></span>
<span class="line"><span>the books back with add_books_to_collection appends them at the end. The items</span></span>
<span class="line"><span>themselves are not deleted.</span></span></code></pre></div><p>The approval is bound to its target, so one obtained for a collection cannot be replayed against another. For a <em>set</em> of targets the binding is a fingerprint of the exact list: an approval for <code>[&quot;a&quot;]</code> does not execute <code>[&quot;a&quot;, &quot;b&quot;]</code>.</p><h2 id="clients-that-cannot-show-a-dialog" tabindex="-1">Clients that cannot show a dialog <a class="header-anchor" href="#clients-that-cannot-show-a-dialog" aria-label="Permalink to “Clients that cannot show a dialog”">​</a></h2><p>Not every MCP client implements elicitation, and a stateless gateway may not be able to speak for the one it is currently serving. Rather than refuse to work — which pushes people towards switching the guard off entirely — the tool falls back to a <strong>two-call token</strong>: the first call returns a random string, the second has to quote it back.</p><p>Be clear about what that proves, because this server is:</p><blockquote><p>the token proves the call was made twice with the same arguments, and nothing more.</p></blockquote><p>A model can read the token out of the first result and call again in the same turn without anybody seeing it. It catches a widened target set; it does not catch a model that was talked into the whole thing. The fallback text says so rather than implying somebody approved.</p><h2 id="switching-the-dialog-off" tabindex="-1">Switching the dialog off <a class="header-anchor" href="#switching-the-dialog-off" aria-label="Permalink to “Switching the dialog off”">​</a></h2><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">ELICITATION</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">false</span></span></code></pre></div><p>Default is <code>true</code>. <code>false</code> does <strong>not</strong> remove the guard — it takes the fallback path above, which means the token. There is no setting in which a guarded call goes unannounced.</p><p>Use it where a dialog is the wrong shape rather than an unwanted one: a scheduled job, a test harness, a client whose dialog interrupts something else.</p><div class="warning custom-block"><p class="custom-block-title">It is deliberately not prefixed</p><p><code>ELICITATION</code> has no <code>AUDIOBOOKSHELF_</code> in front of it, so one <code>export ELICITATION=false</code> — or one <code>-e ELICITATION=false</code> in a compose file — reaches <strong>every</strong> MCP server in that environment, not just this one. That is the point of it and also its risk.</p><p>Two things make it visible rather than silent:</p><ul><li><p>a server started with it off prints one line at startup, in the log of every server it actually reached:</p><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span>audiobookshelf-mcp: ELICITATION=false — guarded tools fall back to the two-call token</span></span></code></pre></div></li><li><p>the fallback text names the server that did not ask, instead of blaming a client that was working fine. :::</p></li></ul><p>Anything other than <code>true</code> or <code>false</code> — <code>1</code>, <code>off</code>, <code>yes</code> — <strong>stops the server</strong> with exit code 1 and a message naming both valid values. This is the only variable in this family that defaults to <em>on</em>: a typo that fell back to the default would leave the dialog running while the operator believed it was off, and there would be nothing to tell them.</p><h2 id="annotations-are-the-other-half-and-they-are-only-a-hint" tabindex="-1">Annotations are the other half, and they are only a hint <a class="header-anchor" href="#annotations-are-the-other-half-and-they-are-only-a-hint" aria-label="Permalink to “Annotations are the other half, and they are only a hint”">​</a></h2><p>Every tool of this server declares all four MCP tool annotations — <code>readOnlyHint</code>, <code>destructiveHint</code>, <code>idempotentHint</code>, <code>openWorldHint</code> — so a client can tell before it calls what a call would do. See <a href="/reference/tools">Tools</a>.</p><p>They are advice, and the specification says so:</p><blockquote><p>clients <strong>MUST</strong> consider tool annotations to be untrusted unless they come from trusted servers</p></blockquote><p>An annotation is something a client may ignore. The dialog is not: it is enforced here, on the server side, and no answer means no change. The two are different claims — the annotation says what a call <em>does</em>, the dialog decides whether it <em>happens</em> — which is why a tool can be marked destructive without being guarded. <code>update_collection</code> is exactly that case: it replaces a name somebody typed, and it is not asked about.</p><h2 id="behind-a-gateway" tabindex="-1">Behind a gateway <a class="header-anchor" href="#behind-a-gateway" aria-label="Permalink to “Behind a gateway”">​</a></h2><p>Both protocol revisions are handled from one code path. On <code>2025-11-25</code> the question is pushed to the client; on <code>2026-07-28</code> there is no server→client channel at all, so the call returns <code>input_required</code>, ends, and the client retries carrying the answer.</p><p>That answer arrives as ordinary request content, which the SDK does not validate — so the state that ties an answer to its question is sealed (HMAC). A reply whose seal does not open, or opens onto a different target, counts as <strong>no answer</strong> and produces a fresh question rather than an error. The likeliest cause is not an attack: it is a gateway that put the server to sleep while the person was reading.</p><p>If you run this behind <a href="https://github.com/ni-c/mcp-hub" target="_blank" rel="noreferrer">mcp-hub</a>, the hub passes elicitation through in both directions; see its <a href="https://ni-c.github.io/mcp-hub/guide/elicitation" target="_blank" rel="noreferrer">elicitation guide</a>.</p></div>`,30)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};