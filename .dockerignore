# Not optional: without it `docker build` can fail while merely READING the build
# context if any file is root-owned 0600 — and it keeps secrets and local state
# away from the daemon.
node_modules
dist
coverage
test
docs
scripts
data
.git
.github
.env
*.log
docker-compose*.yml
